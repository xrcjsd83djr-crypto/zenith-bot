[build]
builder = "nixpacks"
buildCommand = "pnpm install --no-frozen-lockfile && pnpm --filter @workspace/discord-bot run build"

[deploy]
startCommand = "pnpm --filter @workspace/discord-bot run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
